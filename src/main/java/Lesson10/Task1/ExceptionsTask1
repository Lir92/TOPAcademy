Простая задача

Напишите метод parseInteger, который принимает строку и пытается преобразовать её в целое число.

Если преобразование успешно, метод должен вернуть число.

Если возникает исключение NumberFormatException, метод должен вернуть значение по умолчанию (например, -1).

В блоке finally выведите сообщение: "Завершение метода parseInteger"